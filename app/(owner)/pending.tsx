/** Placeholder — see src/ui/NotBuilt.tsx. Replace when O2 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="O2" name="Pending approval" />;
}
