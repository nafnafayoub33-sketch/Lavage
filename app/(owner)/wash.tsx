/** Placeholder — see src/ui/NotBuilt.tsx. Replace when O5 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="O5" name="My wash page" />;
}
