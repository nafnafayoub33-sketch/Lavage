/** Placeholder — see src/ui/NotBuilt.tsx. Replace when O11 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="O11" name="Owner account" />;
}
