/** Placeholder — see src/ui/NotBuilt.tsx. Replace when O1 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="O1" name="Register the car wash" />;
}
