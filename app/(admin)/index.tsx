/** Placeholder — see src/ui/NotBuilt.tsx. Replace when D1 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="D1" name="Dashboard" />;
}
