/** Placeholder — see src/ui/NotBuilt.tsx. Replace when A6 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="A6" name="Profile setup" />;
}
