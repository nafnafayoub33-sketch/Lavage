/** Placeholder — see src/ui/NotBuilt.tsx. Replace when C8 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="C8" name="Confirm and rate" />;
}
