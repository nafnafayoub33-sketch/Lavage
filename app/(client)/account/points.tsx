/** Placeholder — see src/ui/NotBuilt.tsx. Replace when C13 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="C13" name="Points and gifts" />;
}
