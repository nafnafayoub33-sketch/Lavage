/** Placeholder — see src/ui/NotBuilt.tsx. Replace when C12 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="C12" name="My cars" />;
}
