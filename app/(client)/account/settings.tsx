/** Placeholder — see src/ui/NotBuilt.tsx. Replace when C14 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="C14" name="Settings" />;
}
