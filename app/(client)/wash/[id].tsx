/** Placeholder — see src/ui/NotBuilt.tsx. Replace when C3 is built. */
import { NotBuilt } from '@/ui/NotBuilt';

export default function Screen() {
  return <NotBuilt id="C3" name="Car wash page" />;
}
