# Copy to .env. .env is gitignored.

# Where the API lives. Left empty in development so requests go to /api/v1 on
# the same origin and Vite's proxy forwards them — which keeps the refresh
# cookie behaving exactly as it will in production.
VITE_API_BASE=
